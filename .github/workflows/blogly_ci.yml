name: Blogly Deployment CI/CD (Dev & Prod)

on:
  pull_request:
    branches: ["main"]

  workflow_dispatch:

jobs:
  Blogly_Build_Job:
    name: Blogly pipeline builder
    runs-on: windows-latest

    steps:
      - name: check out the repo
        uses: actions/checkout@v4

      - name: setup dotnet
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0

      - name: build project
        run: dotnet build

      - name: format project
        run: dotnet format -v detailed --verify-no-changes

      - name: run tests
        run: dotnet test --no-build

      - name: publish project
        run: dotnet publish Blogly.API/Blogly.API.csproj --configuration Release -o blogly_artifact

  Blogly-CI:
    name: Blogly CI job
    runs-on: windows-latest
    needs: Blogly_Build_Job
    steps:
      - name: upload project artefacts
        uses: actions/upload-artifact@v4
        with:
          name: blogly_artifact
          path: artifacts/

  Blogly-CD:
    name: Blogly CD job
    runs-on: windows-latest 
    needs: Blogly-CI
    steps:
      - name: download project artifact
        uses: actions/download-artifact@v4
        with:
          name: blogly_artifact
          path: artifacts/

      - name: login to azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: 'deploy to Azure webapp'
        uses: azure/webapps-deploy@v2
        with: 
          app-name: app-blogly-dev
          package: artifacts/

      - name: "Signify you're done"
        run: echo "I am done o, Toyin!"
      
        
      

      
